{
  "name": "blueskyexp",
  "version": "0.9.0",
  "description": "",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle --minify --format=esm --platform=node --outdir=dist --out-extension:.js=.mjs src/*.ts",
    "watch": "esbuild --watch --sourcemap=inline --bundle --format=esm --platform=node --outdir=dist --out-extension:.js=.mjs src/*.ts",
    "tl": "tsx src/tl.ts"
  },
  "author": "Susumu OTA",
  "license": "MIT",
  "dependencies": {
    "@atproto/api": "^0.1.3"
  },
  "devDependencies": {
    "@tsconfig/node-lts-strictest-esm": "^18.12.1",
    "@types/node": "^18.14.5",
    "esbuild": "^0.17.10",
    "tsx": "^3.12.3"
  }
}
